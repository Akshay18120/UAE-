# API Configuration
API_BASE_URL=http://localhost:3001/api/v1

# Test User Credentials
TEST_EMAIL=test@example.com
TEST_PASSWORD=testpassword123

# JWT Configuration (if needed for direct token generation)
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=1d

# Database Configuration (if needed for direct database tests)
DATABASE_URL=postgresql://user:password@localhost:5432/procurementpro

# Redis Configuration (if needed for cache testing)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Other Configuration
NODE_ENV=test
